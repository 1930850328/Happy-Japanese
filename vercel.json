{
  "installCommand": "rm -rf node_modules /vercel/.npm && npm ci --workspaces=false --no-audit --no-fund --cache /tmp/hxf-npm-cache",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
